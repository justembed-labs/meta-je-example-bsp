# RSA-signed .swu verification. No compile-time public-key option
# exists -- verification key is runtime-only, via swupdate's own
# mandatory -k argument (see je-swupdate-select).
CONFIG_SIGNED_IMAGES=y
CONFIG_SIGALG_RAWRSA=y
